spring.application.name=claramed-backend
spring.profiles.default=dev
server.port=${PORT:8080}
jwt.secret=${JWT_SECRET:claramed-dev-secret-claramed-dev-secret}
jwt.expiration-ms=${JWT_EXPIRATION_MS:28800000}
app.cors-origins=${CORS_ORIGIN:http://localhost:5173,http://127.0.0.1:5173}
